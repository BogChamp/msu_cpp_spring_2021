# msu_cpp_spring_2021
just download the repository(for example git clone https://github.com/BogChamp/msu_cpp_spring_2021/)

if you want check 01hw, firstly you should download gtest(sudo apt-get install libgtest-dev sudo apt-get install cmake).go to 01 directory, 
then write make. That's all!
